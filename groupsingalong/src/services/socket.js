// import { io } from "socket.io-client";

// // initialize the websocket connection
// const socket = io(
//   process.env.NEXT_PUBLIC_SOCKET_URL || "http://localhost:4000"
// );

// // export the websocket instance to use across the app;
// export default socket;
